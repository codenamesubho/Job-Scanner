venv/
venv.broken/
.env
__pycache__/
*.pyc
*.pyo
.DS_Store
output/*.csv
output/*.json
data/jobs.db
data/jobs.db.bak-*
data/playwright_sessions/*.json
project_progress.md
test_litellm_cliproxy_cache.py

# pytest's tmp_path root, when TMPDIR isn't usable it lands here
pytest-of-*/
.pytest_cache/
.ruff_cache/

# Local tooling / scratch — kept on disk, just not tracked
.mcp.json
.agents/
skills-lock.json
config.yaml
docker-compose.yml
issue.json

# IDE
.idea/

# Claude Code config — keep settings.json (shared), ignore everything else
.claude/*
!.claude/settings.json
